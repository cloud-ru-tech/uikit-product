export enum Variants {
  Primary = 'primary',
  OnDark = 'onDark',
}

export enum Sizes {
  Small = 'small',
  Medium = 'medium',
}

export enum BarHideStrategy {
  Never = 'never',
  Leave = 'leave',
}

export enum Resize {
  None = 'none',
  Horizontal = 'horizontal',
  Vertical = 'vertical',
  Both = 'both',
}
