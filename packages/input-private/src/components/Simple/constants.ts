import { Types as PrivateTypes } from '../Private/constants';

export enum Sizes {
  Small = 'Small',
  Medium = 'Medium',
  Large = 'Large',
}

export enum Types {
  Text = PrivateTypes.Text,
  Password = PrivateTypes.Password,
}
