export enum Variant {
  Primary = 'primary',
  Secondary = 'secondary',
  Tertiary = 'tertiary',
  OnAccent = 'onAccent',
  OnDark = 'onDark',
}

export enum Sizes {
  Small = 'small',
  Medium = 'medium',
}

export enum SizesInPx {
  small = '16px',
  medium = '20px',
}

export enum IconPosition {
  Before = 'before',
  After = 'after',
}
