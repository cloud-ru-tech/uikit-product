export enum Variants {
  Primary = 'primary',
  OnDark = 'onDark',
  OnAccent = 'onAccent',
}

export enum Icons {
  Info = 'info',
  Success = 'success',
  AttentionCritical = 'attention-critical',
  AttentionWarning = 'attention-warning',
  Failed = 'failed',
  Cancel = 'cancel',
  Loading = 'loading',
}
