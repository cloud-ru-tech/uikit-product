import { styled } from '@linaria/react';
import { components as ReactSelectComponents } from 'react-select';

export const StyledSelectContainer = styled(ReactSelectComponents.SelectContainer)`
  width: 20px;
  height: 20px;
`;
